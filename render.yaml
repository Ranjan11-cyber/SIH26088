services:
  - type: web
    name: sahaya-portal
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: GEMINI_API_KEY
        sync: false # Set your Gemini API key in the Render dashboard under Environment Variables
